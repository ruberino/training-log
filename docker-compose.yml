# Runs the production image locally, same as Render, on port 8080.
# For local development with hot reload, use docker-compose.dev.yml instead.
services:
  app:
    build: .
    ports:
      - '8080:8080'
    volumes:
      - data:/data
    env_file: .env

volumes:
  data:
